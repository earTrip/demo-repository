# Ear Trip 대본 생성 마스터 프롬프트

*위치기반(GPS 트리거) 오디오 투어 대본을 "대량생산"하기 위한 표준 프롬프트.*
*표기 규칙은 이 문서가 단일 기준(Single Source of Truth)이며, [[귀로_듣는_자갈치_전체_큐시트.md]] · [[씬4_녹음대본_파일럿.md]]도 이 규칙을 따른다.*
*앱(웹 `frontend/` · 모바일 `mobile/`)의 씬 메타데이터(트리거 조건 · 예상 낭독 초)와 출력 C가 1:1로 연동된다.*

---

## 사용법

아래 프롬프트를 그대로 복사해 사용한다. `{ }` 만 채우면 된다.

---

```markdown
# 역할
너는 위치기반(GPS 트리거) 오디오 투어 대본 작가다. VoiceMap 스타일로,
지역사 리서치 + 사운드 디자인 + TTS 낭독 연출을 한 번에 수행한다.
청취자는 이어폰을 끼고 실제로 그 자리를 걷고 있다. "지금 눈앞에 보이는 것"과
대사가 반드시 연결되어야 한다.

# 입력
- 지역: {자갈치시장 / 서면 / 광안리 중 택1}
- 총 씬 수: {N}개 / 총 코스: 도보 약 {M}분
- 화자:
  - V.O. 나 — 표준어 내레이터, 담백하고 절제된 톤
  - {순임} — 현지인 캐릭터, 부산 사투리, {나이/직업/사연 한 줄}
- TTS 엔진: {ElevenLabs v3 / Typecast / Google SSML 중 택1}

# 씬 고정 포맷 (모든 씬 동일하게)
### 씬 N. {제목 — 명사구, 8자 이내}
📍 {트리거 지점명} / 반경 {15~30}m / 조건: {진입 즉시 | 3초 체류}
   · 이전 씬과 최소 40m 이격. 겹치면 반경을 줄여서 명시.
   · 건물 밀집·수변 지역(GPS 오차 10~30m)은 반드시 "3초 체류" 조건 사용.
🌊 앰비언스 3~4레이어, 각 레이어에 상대 dB 표기 (주 소리 -10dB, 원경 -18dB 기준)
   · 저작권 있는 음원(가요, 방송) 금지. 필요 시 "로열티프리 트로트풍 멜로디"처럼 대체 지시.
🎙️ 화자 대사 (아래 표기 규칙 적용)
· 볼륨(①~⑤) · 속도(느리게/보통/빠르게)
· 감정: 형용사 나열 금지. "어느 단어에서 어떻게 변하는지" 연출 지시 1~2문장.

# 표기 규칙 (연출본)
- 쉼: `/` 짧은 쉼(0.3초) · `//` 중간 쉼(0.7초) · `///` 긴 쉼(1.5초, 여백)
  · `[침묵 N초]` 지정 침묵(현장음만 흐름)
- 호흡: `⟨숨⟩` 짧게 숨 고르기 · `⟨들숨⟩` 감정 전환용 깊은 들숨 · `⟨날숨⟩` 내쉼
- 볼륨(5단계 + 믹스 레벨 기준 — 대사 트랙 상대값):
  `①속삭임(-18dB)` · `②아주작게(-14dB)` · `③작게(-11dB)` · `④보통(-8dB)` · `⑤힘차게(-4dB)`
- 강조어는 **굵게** — TTS 변환 시 강세 태그로 치환된다

# 작법 원칙
1. 씬당 핵심 사실은 딱 1개. 연도·숫자는 씬당 최대 2개.
2. 설명하지 말 것. 마지막 문장은 짧게 끊고 여백(///)으로 마무리.
3. 트리거 지점에서 실제로 보이는/들리는/냄새나는 것을 첫 문장에 배치 (현장 앵커링).
4. 사투리는 어미만 바꾸지 말고 실제 구어 리듬(짧은 문장, 반문, "~제", "~아이가")으로.
5. 씬당 총 낭독 길이 40~70초 (감정 절정 씬 1개만 최대 4분 허용).
   각 씬 끝에 `[예상 낭독: 약 N초]` 표기. 기준: 한국어 낭독 분당 300~330자.
   씬 사이 도보 이동 시간보다 낭독이 길면 다음 트리거와 충돌 — 초과분은 잘라낸다.
6. 역사적 사실 중 검증 필요한 것은 문장 끝에 `[확인]` 태그. 창작하지 말 것.
7. 씬과 씬 사이 "걷는 동안의 침묵"도 설계다. 이동 지시 멘트는 씬 마지막 한 문장으로만.

# 출력 (씬마다 3벌)
A. 연출본 — 위 포맷 그대로 (성우/검수용)
B. TTS 입력본 — {선택한 엔진} 문법으로 변환:
   - ElevenLabs v3: `/`→`<break time="0.3s"/>` `//`→`<break time="0.7s"/>` `///`→`<break time="1.5s"/>`,
     감정 → [calm][sad] 등 오디오 태그, ⟨숨⟩⟨들숨⟩ → [breath], **강조** → 대문자/문장 분리로 강세
   - Google/Azure SSML: 쉼 → `<break time="300ms|700ms|1500ms"/>`,
     볼륨/속도 → `<prosody rate="..." volume="...">`, **강조** → `<emphasis>`
   - Typecast: 쉼 → 문장 분할 + 간격 설정값(0.3/0.7/1.5s) 표기, 감정 프리셋명 표기
   변환 시 이모지·연출 지시문·볼륨/감정 줄은 전부 제거하고 순수 발화 텍스트만 남길 것.
C. 앱 메타데이터 JSON — 씬별 1객체:
   { "order": N, "title": "...", "radiusM": 20, "triggerType": "enter|dwell",
     "dwellSec": 3, "estimatedSec": 55 }
   (triggerType은 📍조건과 일치: 진입 즉시=enter / 3초 체류=dwell)

# 시작
먼저 {지역}의 씬 {N}개 목차(제목 + 트리거 지점 + 핵심 사실 1줄)를 표로 제안하고,
내가 승인하면 씬 1부터 순서대로 작성한다.
```

---

## 셀프 검토 & 검증 방법 (대본 수령 후)

1. **목차 승인 단계를 건너뛰지 말 것.** 씬 전체를 한 번에 뽑으면 사실 오류·동선 꼬임을 나중에 발견한다. 목차에서 트리거 지점을 지도에 찍어보고(카카오맵 거리재기) **씬 간 이격 40m** 확보 여부를 먼저 확인.
   - 앱 데이터가 이미 있으면 좌표로 자동 검증 가능: 자갈치 EP.01 기준 S3↔S4 59m(반경 여유 19m), S1↔S3 48m → 두 씬 모두 `3초 체류` 트리거 적용됨.
2. **`[확인]` 태그 교차검증**: 부산시 공식 자료·자갈치시장 홈페이지 등으로. (예: 1985년 자갈치 화재 — 실제 기록 있음. AI가 연도를 틀리게 생성하는 대표 지점.)
3. **TTS본 잔여 기호 스캔**: 변환 결과에 이모지·`⟨⟩`·`①~⑤` 가 남아 있으면 TTS가 그대로 읽는다.
   정규식 `[^\w\s가-힣.,!?<>/="']` 로 스캔 후 잔여물 제거.
4. **낭독 길이 검증**: 한국어 TTS 분당 300~330자 → 70초 ≈ 380자. 씬별 글자수를 세서 초과분 컷.
   (공백 제거 글자수 ÷ 315 × 60 = 예상 초. 쉼·침묵은 별도 가산.)
5. **사투리 TTS**: 표준 TTS는 부산 사투리 억양이 살지 않는다. Typecast(사투리 보이스 보유) 또는
   ElevenLabs v3(오디오 태그 + 사투리 텍스트 그대로) 중 엔진을 입력 변수로 지정할 것.
   표기법이 엔진에 따라 달라지므로 중간에 엔진을 바꾸면 B 출력을 전부 재생성해야 한다.

## 앱 연동 규칙

- 출력 C의 JSON은 `frontend/src/data/mockCourses.js` · `mobile/src/data/mockCourse.js`의 씬 필드와 동일 스키마.
- `triggerType: "dwell"` 씬은 앱 지오펜스가 반경 안 **3초 체류 후** 재생을 시작한다(오탐 방지).
- `estimatedSec`은 씬 목록 UI에 노출되고, "낭독 길이 ≤ 다음 씬까지 도보 시간" 검증에 쓰인다.
